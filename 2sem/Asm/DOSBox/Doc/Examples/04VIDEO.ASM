;-----------------------------------------------------------------------------
;                 Задание # 4. Файл 04video.asm 
;       То же, что 3, но вывод в определенное место экрана (х;у)
;               (c) 2001 Copyleft  Andreev Andrey
;-----------------------------------------------------------------------------
.model tiny

;-----------------------------------------------------------------------------

x = 2           ;  х и у - константы
y = 3		

;-----------------------------------------------------------------------------
.code
org 100h

start:		mov ax, 4e21h             ; желтый "!" на красном фоне
		mov bx, 0b800h
		mov es, bx
		mov bx, (y * 80 + x) * 2  ; вычисление смещения
		mov es:[bx], ax           ; вывод символа

		mov ax, 4c00h
		int 21h
end start
;-----------------------	Конец...	------------------------------